function execute(connections, errors, results, properties) { 
    print("apresentando");
    results.setPaused(false);
}

function help() {
    print("show - apresenta os resultados na tela com o tempo configurado em <statisticTime>");
}
